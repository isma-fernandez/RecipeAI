name: recipeai
description: "Aplicació receptari, que mitjançant una imatge d’un menjar és capaç de dir-te els ingredients que porta i com es cuina."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  camera: ^0.11.1
  path_provider: ^2.1.1
  provider: ^6.1.5
  shared_preferences: ^2.2.3
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
